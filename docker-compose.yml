version: "3.9"
services:
  platform:
    image: alvanrahimli/postapic
    container_name: postapic
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9010:8080"
    volumes:
      - "./images/:/app/images/"
      - "./db/:/app/db/"

