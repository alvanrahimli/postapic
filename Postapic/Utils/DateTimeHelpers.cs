namespace Postapic.Utils;

public class DateTimeHelpers
{
    public static string GetHumanizedTimestamp(DateTime timestamp)
    {
        // var diff = DateTime.UtcNow - timestamp;
        // if (diff.TotalDays > 3) return timestamp.ToString("dd MMMM yyyy");
        //
        // var isPast = timestamp < DateTime.UtcNow;
        //
        throw new Exception();
    }
}